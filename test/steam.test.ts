import { describe, expect, it } from 'vitest'; import { assertSteamUrl, extractApps } from '@/lib/steam'; import { enrichRequestSchema } from '@/lib/schemas';
describe('Steam discovery parser',()=>{it('extracts and deduplicates app IDs from markup and embedded data',()=>{const html='<a data-ds-appid="10" href="https://store.steampowered.com/app/10"><span class="tab_item_name">Train One</span></a><script>{"appid":20}</script><a href="/app/10">duplicate</a>';expect(extractApps(html)).toEqual([{appId:10,name:'Train One'},{appId:20}]);});it('allows only supported Steam pages',()=>{expect(assertSteamUrl('https://store.steampowered.com/category/trains').pathname).toBe('/category/trains');expect(()=>assertSteamUrl('https://example.com/category/trains')).toThrow();expect(()=>assertSteamUrl('https://store.steampowered.com/app/10')).toThrow();});});
describe('batch validation',()=>{it('caps serverless enrichment batches',()=>{expect(()=>enrichRequestSchema.parse({appIds:Array.from({length:26},(_,i)=>i+1),country:'tr',language:'english'})).toThrow();});});
